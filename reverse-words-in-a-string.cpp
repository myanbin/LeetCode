#include <iostream>
#include <string>
#include <stack>

void reverseWords (std::string &s) {
}

int main () {
	std::string q = "the sky is blue";
	reverseWords(q);
	std::cout << q << std::endl;
	return 0;
}